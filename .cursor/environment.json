{
  "snapshot": "POPULATED_FROM_SETTINGS",
  "install": "npm install && cp -n .env.example apps/api/.env 2>/dev/null || true && npm run db:generate",
  "start": "docker compose up -d postgres 2>/dev/null || true",
  "terminals": [
    {
      "name": "api-dev",
      "command": "npm run dev"
    }
  ]
}
